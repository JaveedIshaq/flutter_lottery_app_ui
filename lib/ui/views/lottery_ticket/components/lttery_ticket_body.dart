import 'package:flutter/material.dart';

class LotteryTicketBody extends StatelessWidget {
  const LotteryTicketBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
